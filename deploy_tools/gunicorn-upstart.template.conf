description "Gunicorn server for SITENAME"

start on net-device-up

stop on shutdown

respawn

setuid ubuntu
chdir /home/ubuntu/PROJECTNAME/PROJECTNAME

exec /home/ubuntu/PROJECTNAME/.pyenv/path/to/gunicorn/binary \
--bind unix:/tmp/SITENAME.socket PROJECTNAME.wsgi:application
